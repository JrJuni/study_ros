# AGENTS.md

## 프로젝트 목적

이 저장소는 로봇을 처음 배우는 학생이 ROS 2, URDF, NVIDIA Isaac Sim과
Omniverse를 단계적으로 익히고, 최종적으로 Isaac Sim 안의 1자유도 관절을
ROS 2로 제어하는 과정을 기록하는 학습 저장소다.

## 독자와 설명 방식

- 독자는 로봇과 Linux를 처음 접하는 고등학생 수준으로 가정한다.
- 문서는 한국어로 쓰고, 새로운 영어 용어는 처음 등장할 때 뜻을 설명한다.
- 명령과 코드를 제시하기 전에 왜 필요한지 설명한다.
- 코드는 짧게 나누고 초보자용 주석을 포함한다.
- 명령의 실행 위치, 예상 결과, 성공 확인 방법을 함께 적는다.
- 어려운 개념은 일상적인 비유를 사용할 수 있지만 실제 기술적 의미와 구분한다.

## 기본 학습 경로

1. 설치와 환경 점검
2. 터미널, Git과 Python 기초
3. ROS 2 핵심 개념과 CLI
4. ROS 2 Python 노드 작성
5. 좌표계, TF와 로봇 모델링
6. URDF와 Xacro 작성
7. Omniverse, USD와 Isaac Sim 기초
8. Articulation과 관절 제어
9. ROS 2 Bridge 연동
10. 1자유도 관절 종합 실습

학습 환경과 진도에 따라 순서를 조정할 수 있지만, 앞 단계에서 사용하는
용어를 설명하지 않은 채 다음 단계로 넘어가지 않는다.

## Git 작업 방식

- 이 저장소는 개인 학습 노트와 실습용이므로 기본적으로 `main`에서 작업한다.
- 작업을 시작하기 전에 로컬 `main`과 `origin/main`을 동기화한다.
- 하나의 학습 내용이나 문서 갱신이 끝나면 관련 파일만 커밋하고 `main`에 푸시한다.
- 별도 브랜치와 Pull Request는 사용자가 명시적으로 요청할 때만 만든다.
- 다른 사람이 남긴 변경이나 현재 학습과 관계없는 파일을 함께 커밋하지 않는다.

## 날짜별 학습 문서 규칙

- 상세 규칙은 `docs/README.md`를 따른다.
- 한국 표준시(Asia/Seoul)를 기준으로 `docs/lesson_YYMMDD.md`를 사용한다.
- 같은 날짜의 문서가 있으면 새 파일을 만들지 말고 기존 문서를 보완한다.
- 각 학습 주제는 반드시 다음 네 부분을 순서대로 포함한다.
  1. 주제
  2. 학습 목표
  3. 내용 설명
  4. 예제 또는 실습
- 실제로 진행한 내용, 확인한 결과, 다음에 할 내용을 구분한다.
- 실행하지 않은 실습을 완료한 것처럼 기록하지 않는다.
- 버전, 지원 환경과 설치법처럼 변할 수 있는 정보는 공식 문서를 확인하고
  출처와 확인 날짜를 남긴다.
- 날짜별 문서를 추가하거나 수정하면 `docs/README.md`의 문서 목록도 갱신한다.

## 코드와 명령 규칙

- 이 프로젝트에서 `ROS`라고만 쓰면 기본적으로 ROS 2를 뜻한다고 밝힌다.
- Python 예제는 PEP 8을 따르고 함수와 중요한 줄에 초보자용 설명을 넣는다.
- URDF 예제는 `link`, `joint`, `origin`, `axis`, `limit`의 역할을 설명한다.
- 위험하거나 시스템 전체에 영향을 주는 명령은 목적과 영향을 먼저 설명한다.
- 환경 확인 없이 특정 Ubuntu, ROS 2 또는 Isaac Sim 버전을 설치하지 않는다.
- 출력 예시는 실제 출력인지 예상 출력인지 표시한다.

## 저장소 구조

```text
study_ros/
├── AGENTS.md
├── BACKLOG.md
├── docs/
│   ├── README.md
│   └── lesson_YYMMDD.md
└── .agents/skills/update-ros-lessons/
```

실습 코드가 생기면 목적에 따라 `ros2_ws/src/`, `robot_description/`,
`isaac_sim/` 아래에 배치하고 해당 학습 문서에서 연결한다.
